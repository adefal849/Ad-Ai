[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Redirige /webhook vers la fonction, pour avoir une URL propre
[[redirects]]
  from = "/webhook"
  to = "/.netlify/functions/webhook"
  status = 200
  force = true
